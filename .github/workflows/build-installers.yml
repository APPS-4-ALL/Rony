name: Build Installers

# Build the Windows .exe and macOS .dmg installers.
# Runs on demand, and automatically when you push a version tag like v1.0.0.
#
# CODE SIGNING (scaffolded — activates automatically once you add the secrets):
#   The signing credentials below are read from repository secrets. While those
#   secrets are EMPTY, electron-builder simply produces UNSIGNED installers (a
#   warning, not a failure), so this workflow works today. The day you add the
#   certs as secrets (see docs/RELEASE.md), the same builds become signed — no
#   workflow change needed. macOS NOTARIZATION additionally needs a one-line flip
#   in electron-builder.yml (`notarize`), also documented in docs/RELEASE.md.
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            name: windows
            build: npm run build:win -- --publish never
            artifact: dist/*-setup.exe
          - os: macos-latest
            name: macos
            build: npm run build:mac -- --publish never
            artifact: dist/*.dmg

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: npm

      # Full install (runs postinstall = electron-builder install-app-deps),
      # which rebuilds the native better-sqlite3 module for this OS.
      - name: Install dependencies
        run: npm ci

      - name: Build installer
        run: ${{ matrix.build }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # --- Code-signing certificate (.p12/.pfx, base64-encoded) + password ---
          # Per-OS so Windows and macOS can use different certs. Empty == unsigned.
          CSC_LINK: ${{ matrix.name == 'windows' && secrets.WIN_CSC_LINK || secrets.MAC_CSC_LINK }}
          CSC_KEY_PASSWORD: ${{ matrix.name == 'windows' && secrets.WIN_CSC_KEY_PASSWORD || secrets.MAC_CSC_KEY_PASSWORD }}
          # --- Apple notarization (macOS only; ignored on Windows) ---
          # Used once electron-builder.yml has `notarize` enabled. Empty == skipped.
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}

      - name: Upload installer
        uses: actions/upload-artifact@v4
        with:
          name: rony-${{ matrix.name }}
          path: ${{ matrix.artifact }}
          if-no-files-found: error
